<Window x:Class="ПрогЛабор.Views.QualityTestView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Испытание готовой продукции" Height="550" Width="750"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#F0F0F0" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Номер испытания:" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding Test.TestNumber}" Margin="0,0,20,0"/>
                <TextBlock Text="Статус:" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding Test.Status}" Margin="0,0,20,0"/>
                <TextBlock Text="Приоритет:" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding Test.Priority}"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Parameters}" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Параметр" Binding="{Binding ParameterName}" Width="150"/>
                <DataGridTextColumn Header="Норматив" Binding="{Binding StandardText}" Width="150"/>
                <DataGridTextColumn Header="Норматив (мин)" Binding="{Binding StandardValueMin}" Width="100"/>
                <DataGridTextColumn Header="Норматив (макс)" Binding="{Binding StandardValueMax}" Width="100"/>
                <DataGridTextColumn Header="Ед. изм." Binding="{Binding Unit}" Width="80"/>
                <DataGridTemplateColumn Header="Факт. значение" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding MeasuredValue, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Результат" Binding="{Binding Result}" Width="100"/>
                <DataGridTextColumn Header="Комментарий" Binding="{Binding AnalystComment}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="💾 Сохранить все" Command="{Binding SaveAllResultsCommand}" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="✅ Завершить испытание" Command="{Binding CompleteTestCommand}" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="📄 Протокол" Command="{Binding ExportProtocolCommand}" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Закрыть" IsCancel="True" Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>