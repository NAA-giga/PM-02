<Window x:Class="ПрилТехно.Views.TechCardEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование технологической карты" Height="650" Width="850"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Основные поля -->
        <GroupBox Header="Общие сведения" Grid.Row="0" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Продукт:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="0" Grid.Column="1" 
                          ItemsSource="{Binding Products}" 
                          SelectedItem="{Binding SelectedProduct}"
                          DisplayMemberPath="Name"
                          IsEnabled="{Binding IsProductSelectable}"
                          Margin="5,0"/>

                <TextBlock Text="Название карты:" Grid.Row="0" Grid.Column="2" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding TechCardName, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="5,0" Height="28"/>

                <TextBlock Text="Описание:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
                <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" 
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" TextWrapping="Wrap" Height="60" Margin="5,5,5,0"/>
            </Grid>
        </GroupBox>

        <!-- Таблица шагов -->
        <GroupBox Header="Технологические шаги" Grid.Row="1" Grid.RowSpan="2" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                    <Button Content="➕ Добавить шаг" Command="{Binding AddStepCommand}" 
                            Margin="0,0,10,0" Padding="8,3"/>
                    <Button Content="✏️ Редактировать" Command="{Binding EditStepCommand}" 
                            CommandParameter="{Binding SelectedStep}" Margin="0,0,10,0" Padding="8,3"/>
                    <Button Content="🗑️ Удалить" Command="{Binding RemoveStepCommand}" 
                            CommandParameter="{Binding SelectedStep}" Margin="0,0,10,0" Padding="8,3"/>
                </StackPanel>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Steps}" 
                          SelectedItem="{Binding SelectedStep}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          CanUserAddRows="False" CanUserDeleteRows="False"
                          CanUserReorderColumns="False" CanUserSortColumns="True"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="№" Binding="{Binding StepOrder}" Width="50"/>
                        <DataGridTextColumn Header="Наименование шага" Binding="{Binding StepName}" Width="150"/>
                        <DataGridTextColumn Header="Тип" Binding="{Binding StepType}" Width="100"/>
                        <DataGridTextColumn Header="Оборудование" Binding="{Binding EquipmentName}" Width="120"/>
                        <DataGridTextColumn Header="Температура, °C" Binding="{Binding PlannedTempC}" Width="100"/>
                        <DataGridTextColumn Header="Давление, бар" Binding="{Binding PlannedPressureBar}" Width="100"/>
                        <DataGridTextColumn Header="Длительность, мин" Binding="{Binding PlannedDurationMin}" Width="100"/>
                        <DataGridTextColumn Header="Обязательный" Binding="{Binding IsMandatory}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- Кнопки действий -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="100" Margin="0,0,10,0" Padding="8,3"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100" Padding="8,3"/>
        </StackPanel>
    </Grid>
</Window>