<UserControl x:Class="ПрилТехно.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель инструментов -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ Добавить" Command="{Binding CreateProductCommand}" 
                        Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="✏️ Редактировать" Command="{Binding EditProductCommand}"
                        CommandParameter="{Binding SelectedItem, ElementName=ProductsGrid}"
                        Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="🗑️ Архивировать" Command="{Binding ArchiveProductCommand}"
                        CommandParameter="{Binding SelectedItem, ElementName=ProductsGrid}"
                        Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="🔄 Обновить" Command="{Binding RefreshCommand}"
                        Padding="10,5"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="10,0"/>
                <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="5,0"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Width="200" Margin="5,0" />
            </StackPanel>
        </Border>

        <!-- DataGrid продуктов -->
        <DataGrid x:Name="ProductsGrid" Grid.Row="1" 
                  ItemsSource="{Binding Products}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Код" Binding="{Binding Code}" Width="100"/>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding ProductType}" Width="120"/>
                <DataGridTextColumn Header="Форма выпуска" Binding="{Binding FormType}" Width="120"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>