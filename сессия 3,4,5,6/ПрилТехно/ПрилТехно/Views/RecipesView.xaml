<UserControl x:Class="ПрилТехно.Views.RecipesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#F5F5F5" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ Создать" Command="{Binding CreateRecipeCommand}" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="✏️ Редактировать" Command="{Binding EditRecipeCommand}" CommandParameter="{Binding SelectedItem, ElementName=RecipesGrid}" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="✅ Утвердить" Command="{Binding ApproveRecipeCommand}" CommandParameter="{Binding SelectedItem, ElementName=RecipesGrid}" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="📦 Архивировать" Command="{Binding ArchiveRecipeCommand}" CommandParameter="{Binding SelectedItem, ElementName=RecipesGrid}" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="🔄 Обновить" Command="{Binding RefreshCommand}" Padding="10,5"/>
            </StackPanel>
        </Border>
        <DataGrid x:Name="RecipesGrid" Grid.Row="1" ItemsSource="{Binding Recipes}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Продукт" Binding="{Binding ProductName}" Width="150"/>
                <DataGridTextColumn Header="Версия" Binding="{Binding Version}" Width="80"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Создана" Binding="{Binding CreatedAt, StringFormat='dd.MM.yyyy'}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>