<Window x:Class="ПрилТехно.Views.ComponentEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование компонента" Height="350" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Параметры компонента" FontWeight="Bold" FontSize="16" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Сырьё:" Width="100" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding RawMaterials}" 
                      SelectedItem="{Binding SelectedRawMaterial}"
                      DisplayMemberPath="Name"
                      Width="250" Margin="10,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Доля, %:" Width="100" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Percentage}" Width="250" Margin="10,0"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Порядок загрузки:" Width="100" VerticalAlignment="Center"/>
            <TextBox Text="{Binding LoadOrder}" Width="250" Margin="10,0"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Допуск отклонения -:" Width="100" VerticalAlignment="Center"/>
            <TextBox Text="{Binding ToleranceMin}" Width="250" Margin="10,0"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Допуск отклонения +:" Width="100" VerticalAlignment="Center"/>
            <TextBox Text="{Binding ToleranceMax}" Width="250" Margin="10,0"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="80"/>
        </StackPanel>
    </Grid>
</Window>