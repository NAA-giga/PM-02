<UserControl x:Class="ПрилТехно.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель – список отчётов -->
        <Border Grid.Column="0" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="1" CornerRadius="5">
            <ListBox x:Name="ReportListBox" 
         SelectedValue="{Binding SelectedReport}" 
         SelectedValuePath="Content" 
         Margin="5">
                <ListBoxItem Content="Отчёт по заказам"/>
                <ListBoxItem Content="Отчёт по партиям за период"/>
                <ListBoxItem Content="Отчёт по отклонениям"/>
                <ListBoxItem Content="Отчёт по использованию рецептур"/>
                <ListBoxItem Content="Отчёт по событиям экструдера"/>
                <ListBoxItem Content="Отчёт по лабораторным блокировкам"/>
                <ListBoxItem Content="Журнал событий системы"/>
            </ListBox>
        </Border>

        <!-- Правая область – параметры и результат -->
        <StackPanel Grid.Column="1" Margin="10">
            <GroupBox Header="Параметры отчёта" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" Margin="10">
                    <TextBlock Text="Период:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <DatePicker SelectedDate="{Binding StartDate, Mode=TwoWay}" Width="120" Margin="0,0,10,0"/>
                    <TextBlock Text="—" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <DatePicker SelectedDate="{Binding EndDate, Mode=TwoWay}" Width="120" Margin="0,0,20,0"/>
                    <Button Content="Сформировать" Command="{Binding GenerateReportCommand}" 
                            Width="100" Padding="8,3" IsEnabled="{Binding IsNotLoading}"/>
                    <Button Content="Экспорт в Excel" Command="{Binding ExportCommand}" 
                            Width="120" Margin="10,0,0,0" Padding="8,3" IsEnabled="{Binding CanExport}"/>
                </StackPanel>
            </GroupBox>

            <ProgressBar IsIndeterminate="True" Height="5">
                <ProgressBar.Style>
                    <Style TargetType="ProgressBar">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ProgressBar.Style>
            </ProgressBar>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <DataGrid ItemsSource="{Binding ReportData}" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Номер партии" Binding="{Binding НомерПартии}"/>
                        <DataGridTextColumn Header="Продукт" Binding="{Binding Продукт}"/>
                        <DataGridTextColumn Header="Дата запуска" Binding="{Binding ДатаЗапуска}"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Статус}"/>
                        <DataGridTextColumn Header="План, кг" Binding="{Binding ПланКг}"/>
                        <DataGridTextColumn Header="Факт, кг" Binding="{Binding ФактКг}"/>
                        <DataGridTextColumn Header="Лаб. решение" Binding="{Binding ЛабРешение}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>