<Window x:Class="ПрилТехно.Views.RecipesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ПрилТехно.Views"
        mc:Ignorable="d"
        Title="RecipesView" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель кнопок -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Обновить" Command="{Binding LoadDataCommand}" Width="100" Margin="0,0,10,0"/>
            <Button Content="Новая рецептура" Command="{Binding NewRecipeCommand}" Width="150" Margin="0,0,10,0"/>
        </StackPanel>

        <!-- Фильтры -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" Padding="5" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Продукт:" VerticalAlignment="Center" Margin="5,0"/>
                <ComboBox ItemsSource="{Binding Products}" DisplayMemberPath="Name" 
                          SelectedValuePath="Id" SelectedValue="{Binding ProductFilter}"
                          Width="200" Margin="5,0"/>
                <TextBlock Text="Статус:" VerticalAlignment="Center" Margin="15,0,5,0"/>
                <ComboBox SelectedItem="{Binding StatusFilter}" Width="120" Margin="5,0">
                    <ComboBoxItem Content="Все"/>
                    <ComboBoxItem Content="draft"/>
                    <ComboBoxItem Content="pending"/>
                    <ComboBoxItem Content="approved"/>
                    <ComboBoxItem Content="archived"/>
                </ComboBox>
                <Button Content="Применить" Command="{Binding ApplyFilterCommand}" Width="80" Margin="15,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Таблица рецептур -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Recipes}" 
                  SelectedItem="{Binding SelectedRecipe}" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  SelectionMode="Single" RowHeaderWidth="0">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Редактировать" Command="{Binding EditRecipeCommand}" 
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource Self}}"/>
                    <MenuItem Header="Утвердить" Command="{Binding ApproveRecipeCommand}" 
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource Self}}"/>
                    <MenuItem Header="Архивировать" Command="{Binding ArchiveRecipeCommand}" 
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource Self}}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Продукт" Binding="{Binding ProductName}" Width="150"/>
                <DataGridTextColumn Header="Версия" Binding="{Binding Version}" Width="80"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Дата создания" Binding="{Binding CreatedAt, StringFormat='dd.MM.yyyy'}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Индикатор загрузки -->
        <Border Grid.Row="2" Background="#80FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                <TextBlock Text="Загрузка..." Margin="0,10,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
